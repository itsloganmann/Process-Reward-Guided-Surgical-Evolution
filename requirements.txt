# Core dependencies for PRSE
# Pin protobuf < 5 to avoid 'MessageFactory' GetPrototype removal in 5.x.
# The pure-Python fallback (PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python)
# restores the legacy API in 4.x but was removed entirely in 5.x.
protobuf>=3.20,<5
datasets
pandas
matplotlib
seaborn
